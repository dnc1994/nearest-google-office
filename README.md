# Nearest Google Office

Interactive U.S. heatmap showing approximate straight-line distance to the nearest listed Google office.

Open `index.html` directly or serve the folder with a static server.

